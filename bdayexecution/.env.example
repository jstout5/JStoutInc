# Copy this to .env and fill in your values

# Email (Gmail — create an App Password at myaccount.google.com/apppasswords)
SMTP_EMAIL=frostbytehero@gmail.com
SMTP_PASSWORD=your_gmail_app_password

# Walmart account (the account that does the buying)
WALMART_EMAIL=your_walmart_email@gmail.com
WALMART_PASSWORD=your_walmart_password

# Stripe webhook secret (from Stripe Dashboard > Webhooks)
STRIPE_WEBHOOK_SECRET=whsec_

# Admin approval base URL (where this server runs)
BASE_URL=http://localhost:8000
